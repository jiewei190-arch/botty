"""Streamlit monitoring dashboard.

Read-only in this version: it shows what the analysis and strategy layers see —
charts, indicators, and proposed setups — but places no orders. Order placement
arrives with the execution layer in Phase 7, and the dashboard's trade controls
with it.

Launch it with ``python main.py dashboard``.
"""
